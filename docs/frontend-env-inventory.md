# Frontend env inventory (`quiz_frontend`)

> Generated by **ET-1.5-A1** — Epic 1.5, Batch A.
> Source command: `grep -rn "process\.env\.NEXT_PUBLIC_" quiz_frontend/`

## Summary

- **Distinct variables:** 2
- **Reference sites:** 4
- **Files covered:** `quiz_frontend/src/**/*.{ts,tsx}` (no `.js`/`.jsx` matches; no `app/` or `pages/` router usage of these vars directly — those files are in `src/shared/lib/`).

## Inventory

| Variable | File | Line | Inferred type | Required? | Default fallback in code | One-line purpose |
|---|---|---|---|---|---|---|
| `NEXT_PUBLIC_API_URL` | `quiz_frontend/src/lib/api/core/custom-instance.ts` | 25 | string (URL) | optional (has fallback) | `http://localhost:8080` | Base URL for authenticated SDK custom-instance requests. |
| `NEXT_PUBLIC_API_URL` | `quiz_frontend/src/lib/api/core/auth-only-instance.ts` | 13 | string (URL) | optional (has fallback) | `http://localhost:8080` | Base URL for auth-only SDK instance requests. |
| `NEXT_PUBLIC_API_URL` | `quiz_frontend/src/shared/lib/api/server.ts` | 6 | string (URL) | optional (has fallback) | `http://localhost:8080` | Server-side helper that prefixes the API version segment (`/api/v1`). |
| `NEXT_PUBLIC_API_URL` | `quiz_frontend/src/shared/lib/api/client.ts` | 10 | string (URL) | optional (has fallback) | `http://localhost:8080` | Base URL for browser-side SDK client. |
| `NEXT_PUBLIC_SITE_URL` | `quiz_frontend/src/shared/lib/seo.ts` | 9 | string (URL) | optional (has `??` fallback) | `DEFAULT_SITE_URL` constant | Absolute base URL used by SEO helpers (canonical, OG, etc.). |

## Cross-reference against existing `quiz_frontend/.env`

`quiz_frontend/.env` currently contains:

```env
NEXT_PUBLIC_API_URL=http://localhost:8080
```

- `NEXT_PUBLIC_API_URL` — present (matches the in-code fallback).
- `NEXT_PUBLIC_SITE_URL` — **not present** in `.env`; currently relies on the in-code `DEFAULT_SITE_URL` constant.

## Notes for ET-1.5-A2

Both variables have working in-code fallbacks, so they are technically optional. ET-1.5-A2 must still emit them in `quiz_frontend/.env.example` so a new dev can override the host (e.g. to point at a staging backend) without editing `.env` against an undocumented contract.