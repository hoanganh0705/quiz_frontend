/**
 * Categories Admin Components
 *
 * These components handle the admin-specific UI for the categories feature.
 * They are separated from the public-facing category components to keep
 * the admin area isolated and maintainable.
 */

// Re-export shared types used by admin pages
export type { Category } from '../types'
