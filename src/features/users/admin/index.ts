/**
 * Users Admin Components
 *
 * These components handle the admin-specific UI for the users feature.
 * They are separated from the public-facing user components to keep
 * the admin area isolated and maintainable.
 */

// Re-export shared types used by admin pages
export type { CurrentUserResponse } from '../types'
