/**
 * Quizzes Admin Components
 *
 * These components handle the admin-specific UI for the quizzes feature.
 * They are separated from the public-facing quiz components to keep
 * the admin area isolated and maintainable.
 */

// Re-export shared types used by admin pages
export type { Quiz } from '../types'
