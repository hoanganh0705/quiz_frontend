/**
 * Tags Admin Components
 *
 * These components handle the admin-specific UI for the tags feature.
 * They are separated from the public-facing tag components to keep
 * the admin area isolated and maintainable.
 */

// Re-export shared types used by admin pages
export type { Tag } from '../types'
