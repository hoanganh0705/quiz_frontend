// Re-exports the discussion domain types from the canonical API module.
// Previously re-exported from a separate `./discussion` module that was
// deleted in Phase 1 as a dead duplicate.
export * from '../api/discussions'